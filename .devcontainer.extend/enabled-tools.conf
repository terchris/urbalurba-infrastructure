# enabled-tools.conf - Tools to install when the container is created
# Add tool script names (one per line) to enable automatic installation
# Lines starting with # are comments
#
# Example:
#   install-dev-python.sh
#   install-dev-typescript.sh
#
# Use 'dev-setup' inside the container to manage tools interactively.
