# UIS Default Configuration
# Copy to .uis.secrets/defaults.env and edit
#
# These values are used when creating VMs and for default service configuration.

# VM user account (used for SSH access via Ansible)
VM_USERNAME="ansible"
VM_PASSWORD=""

# Default admin email (used by various services)
ADMIN_EMAIL=""
