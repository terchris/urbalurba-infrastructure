apiVersion: v1
kind: Secret
metadata:
  name: urbalurba-secrets
  namespace: argocd
type: Opaque
stringData:
  # ================================================================
  # ARGOCD ADMIN CREDENTIALS - NOVICE USER FRIENDLY
  # ================================================================
  # Simple, predictable credentials that novice users can easily use
  ARGOCD_ADMIN_USERNAME: "admin"
  ARGOCD_ADMIN_PASSWORD: "SecretPassword2"